#Amplify Console build file
version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd workshop-hugo/instructions
        - hugo --enableGitInfo
  artifacts:
    baseDirectory: workshop-hugo/instructions/public
    files:
      - '**/*'
  cache:
    paths: []
