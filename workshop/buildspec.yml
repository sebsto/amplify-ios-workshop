#Amplify Console build file
version: 0.1
frontend:
  phases:
    build:
      commands:
        - cd BOA332/instructions
        - hugo --enableGitInfo
  artifacts:
    baseDirectory: workshop/instructions/public
    files:
      - '**/*'
  cache:
    paths: []
